// API URLs
export const API_BASE_URL = ''; // Placeholder for future API integration

// Locale settings
export const LOCALE = 'he-IL';

// Status labels
export const STATUS_LABELS = {
  success: 'Success',
  error: 'Error',
  failed: 'Failed',
  canceled: 'Canceled',
  running: 'Running',
  waiting: 'Waiting'
};

// Filter options
export const FILTER_OPTIONS = {
  ALL: 'all',
  ACTIVE: 'active',
  INACTIVE: 'inactive'
};

// Chart configuration
export const CHART_CONFIG = {
  donutRadius: 70,
  donutStrokeWidth: 20,
  donutCenterX: 90,
  donutCenterY: 90,
  timelineMaxHeight: 140,
  timelineMaxBars: 30
};
